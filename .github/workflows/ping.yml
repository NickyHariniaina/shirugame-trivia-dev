name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # ping toutes les 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://shirugame.onrender.com > /dev/null
        
